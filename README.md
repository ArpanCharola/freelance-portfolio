# Arpan Charola Portfolio

A Vite + React portfolio for Arpan Charola, focused on freelance frontend development, full-stack apps, and polished web experiences.

## Run Locally

**Prerequisites:** Node.js

1. Install dependencies:
   `npm install`
2. Start the development server:
   `npm run dev`
3. Build for production:
   `npm run build`
